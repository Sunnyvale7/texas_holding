# 德州神算子 (Texas Hold'em Engine)

这是一个基于 FastAPI 和 WebSocket 开发的在线德州扑克游戏引擎，支持人类玩家对战、智能机器人 (Medium/Simple AI) 以及强化学习训练的高级 AI。

## 核心功能
- **多玩家在线对战**：支持 2-9 人同场竞技。
- **智能 AI 系统**：
  - `Simple`: 随机/上头型。
  - `Medium`: 基于胜率模拟和诈唬逻辑。
  - `Hard`: 基于 RL (强化学习) 训练的模型。
- **贷款机制**：筹码输光后支持申请贷款继续游戏。
- **盈亏统计**：实时显示玩家本场战绩，离席时生成总结报告。
- **自动管理**：无人房间自动重置，支持多房间隔离。

## 快速开始

### 1. 本地运行
```bash
# 安装依赖
pip install -r requirements.txt

# 启动服务器
uvicorn holdem_server:app --reload --host 0.0.0.0 --port 8000
```
然后在浏览器访问 `http://localhost:8000`。

### 2. 服务器部署
```bash
nohup uvicorn holdem_server:app --host 0.0.0.0 --port 8000 > server.log 2>&1 &
```

## AI 训练
本项目包含三个训练脚本：
- `train_rl.py`: 基础强化学习训练。
- `train_rl_hard.py`: 针对中级机器人的高强度对决训练。
- `train_rl_self_play.py`: 自博弈进化训练。

---
Enjoy the game! 🃏





